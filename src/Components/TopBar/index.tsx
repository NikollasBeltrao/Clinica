import React from 'react';
import './topBar.css';

const TopBar: React.FC = (props) => {

    return (<>
        <nav>
            <ul>
                <li></li>
            </ul>
        </nav>
    </>);
}

export default TopBar;